name: Composite Action
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up python
        uses: gowridurgad/sample-python@main
