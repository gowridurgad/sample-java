name: Gitea CI

on:
 workflow_dispatch:

jobs:
  build:
    runs-on: mocos-latest
    steps:
      - name: setup java
        uses: actions/setup-java@v4
        with:
          distribution: 'semeru'
          java-version: '21'
         
