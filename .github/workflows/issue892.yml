name: Gitea CI

on:
 workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: setup java
        uses: actions/setup-java@v4
        with:
          distribution: 'graalvm'
          java-version: '17.0.12'
          check-latest: false
